---
permalink: /robots.txt
eleventyExcludeFromCollections: true
---
{%- if noindex %}
# Preview deployment — NOINDEX=1 is set, so crawlers are kept out entirely.
# Unset NOINDEX on the production deployment to allow indexing.
User-agent: *
Disallow: /
{%- else %}
User-agent: *
Allow: /
Disallow: /404.html
Disallow: /contact/thank-you/

Sitemap: {{ "/sitemap.xml" | absUrl(siteUrl) }}
{%- endif %}
